// User Roles
const ROLES = {
  USER: "user",
  ADMIN: "admin",
  EVALUATOR: "evaluator",
};

// Idea Status
const IDEA_STATUS = {
  PENDING: "pending",
  APPROVED: "approved",
  REJECTED: "rejected",
};

// Default Score Range
const SCORE = {
  MIN: 0,
  MAX: 10,
};

module.exports = {
  ROLES,
  IDEA_STATUS,
  SCORE,
};